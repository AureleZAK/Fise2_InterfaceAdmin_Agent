stages:
  - setup
  - build


setup:
  stage: setup
  script:
    - apk add --no-cache docker

build:
  stage: build
  script:
    - adduser $USER docker
    - docker build --platform linux/amd64 -t image_agent .