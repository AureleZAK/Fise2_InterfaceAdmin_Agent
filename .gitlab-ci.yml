stages:
  - setup
  - build


setup:
  stage: setup
  script:
    - apt-get update -qy
    - apt-get install -y docker.io

build:
  stage: build
  script:
    - docker build --platform linux/amd64 -t image_agent .