stages:
  - build

variables:
  DOCKER_IMAGE_NAME: "mon-projet"

before_script:
  - docker info

build_image:
  stage: build
  script:
    - docker build -t $DOCKER_IMAGE_NAME:latest .
    - docker run $DOCKER_IMAGE_NAME:latest npm test

