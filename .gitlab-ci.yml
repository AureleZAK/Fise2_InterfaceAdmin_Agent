stages:
  - setup
  - check_docker

setup:
  stage: setup
  script:
    - apk add --no-cache docker

check_docker:
  stage: check_docker
  services:
    - docker:dind
  script:
    - docker --version
    - docker info
