check_docker:
  stage: check_docker
  image: docker:latest
  services:
    - name: docker:dind
      alias: docker
  script:
    - export DOCKER_HOST=tcp://docker:2375
    - docker --version
    - docker info
